package phonebookprogram;

public class phonebookprogram {
    
    public static void addEntry(String[] names, String[] numbers, String name, String number) {

        int entries = 0; //entries is a global variable
        if (entries != 100){

            names[entries] = newName; //imagine new name is an input from user
            numbers[entries] = newNumber //imagine new number is a input from user
            entries = entries + 1;

        }

        else System.out.println("Address Book Full");

    }

}
